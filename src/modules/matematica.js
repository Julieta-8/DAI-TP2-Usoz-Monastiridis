const restarDosNumeros = function(num1,num2){
      return num1 - num2;
} 

const sumarDosNumeros = function(num1,num2){
      return num1 + num2;
} 

const Cuadrado = num1 => num1 * num1;

const cubo = num1 => num1 * num1 * num1;


const pi = 3.14159265359;

export {Cuadrado,cubo,sumarDosNumeros,restarDosNumeros,pi}
//  Crear un módulo matemática /src/modules/matematica.js que tenga 
// 4 funciones (dos de ellas arrow functions) y una constante (PI). Exportarlas y utilizarlas en el programa principal.